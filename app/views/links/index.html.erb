<div class="form">

  <% @links.each do |link| %>
  <p>
    <%= link_to 'https://tranquil-cove-31053/' + link.new_url, link_path(:id => link.new_url ) %> => <%= link.given_url %>
  </p>
  <% end %>
  <%= link_to "New Url", new_link_path %>
</div>
