<div class="form">
  <% @link.errors.each do |attr, msg| %>
  <%= msg %> on <%= attr %>
  <% end %>

  <%= form_for @link do |f| %>
  <p style="font-size:2em">
    <%= f.label :enter_url %><br>
  </p>
    <%= f.text_field :given_url, placeholder: 'Type Your Url' %>
  <p>
    <%= f.hidden_field :new_url, :value => @link.new_url %>
    <br>
    <%= f.submit "New Url" %>
  </p>
  <% end %>
</div>
